<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Demo Bank | Reset Requested</title>
    <link rel="stylesheet" href="/static/styles.css" />
  </head>
  <body class="page page--auth">
    <header class="header">
      <h1 class="header__logo">Demo Bank</h1>
    </header>
    <main class="auth">
      <section class="card">
        <h2 class="card__title">Check your inbox</h2>
        <% if (found) { %>
          <p class="card__subtitle" data-testid="forgot-password-success">
            If <strong><%= email %></strong> matches an account, you’ll receive a reset link shortly.
          </p>
        <% } else { %>
          <p class="card__subtitle" data-testid="forgot-password-success">
            We didn’t find <strong><%= email %></strong>, but if it’s a valid account you’ll still receive a reset email.
          </p>
        <% } %>
        <div class="auth__links">
          <a href="/login">Return to sign in</a>
        </div>
      </section>
    </main>
  </body>
</html>
