<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Demo Bank | Forgot Password</title>
    <link rel="stylesheet" href="/static/styles.css" />
  </head>
  <body class="page page--auth">
    <header class="header">
      <h1 class="header__logo">Demo Bank</h1>
    </header>
    <main class="auth">
      <section class="card">
        <h2 class="card__title">Reset your password</h2>
        <p class="card__subtitle">
          Enter the email address associated with your account. We'll send reset instructions if it matches our records.
        </p>
        <form method="post" action="/forgot-password" data-testid="forgot-password-form">
          <label class="form__label" for="email">Email address</label>
          <input
            class="form__input"
            id="email"
            name="email"
            type="email"
            autocomplete="email"
            data-testid="forgot-password-email"
            required
          />
          <button class="button button--primary" type="submit" data-testid="forgot-password-submit">
            Send Reset Link
          </button>
        </form>
        <div class="auth__links">
          <a href="/login">Back to sign in</a>
        </div>
      </section>
    </main>
  </body>
</html>
