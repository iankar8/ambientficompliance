{
  "name": "@arcanared/artifact-processor",
  "version": "0.1.0",
  "private": true,
  "main": "dist/artifact-processor/src/index.js",
  "types": "dist/artifact-processor/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint 'src/**/*.ts'",
    "format": "prettier --write 'src/**/*.ts'",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@arcanared/shared": "file:../shared"
  }
}
